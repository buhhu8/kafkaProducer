create table if not exists newFile (
                                       id int primary key,
                                       fileName varchar not null,
                                       fileCreateDate DATE
);
